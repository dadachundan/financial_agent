# Available Skills — Graph View

Snapshot of every skill registered for this project, grouped by role and dependency chain.

> **Keep this file in sync.** Whenever a skill is added, removed, or its `## Prerequisites` block changes under `.claude/skills/`, update this file in the same commit and bump the "Last updated" date below. See the [Maintenance](#maintenance) section at the bottom for the checklist.

Last updated: 2026-06-09 (round 17 — **`company-research` now searches the local zsxq institute-research library (`db/zsxq.db`, ~6,900 broker PDFs) FIRST for the sell-side view — mirroring round 16's `theme-research` integration.** A new **Step 0.7** (always-run, all domiciles) sweeps the library by every alias (ticker / English name / 中文名 / supply-chain & competitor terms) via the read-only `zsxq-analyze/find_pdf.py`, before the website pass; the curated `summary` column (翻译精华) is mined first since it usually already carries broker + rating + price target + valuation basis + thesis without opening the (often image-only) PDF — OCR + `extract_pdf.py` only when a verbatim quote/number is needed. A new top-level § "Local institute-research library" documents the two-layer search, what the library unlocks per section (Valuation PT/consensus, Industry channel checks, Competitive share, TAM build, Risk bear-case), a worked NVDA example (`file_id 812488522252442` → MS Overweight / PT $288 / 2027E EPS $13.08 × 22× PE), the density bar (≥3–6 cites, ≥1 in §2 and §9), and the fallback downloader top-up (`download/zsxq_downloader.py --count 100 --query <ticker|lite>`). **Citation discipline is the strict part:** everything from zsxq is sell-side → labeled `*Analyst view:* / *分析师观点：*`, never blended into a filing citation (reinforces the existing "do NOT misattribute sell-side opinions to filings" guardrail), cited to the **verified-live `http://xs-macbook-air.local:5001/zsxq/pdf-viewer/<file_id>` route** (the old `/zsxq-pdf/<id>` form is a dead 404 — Step 10.1 now flags it), with broker + date + page in the link text and every number string-matched to the summary/OCR. Touch-points updated: frontmatter description, Source-hierarchy item 6, Data-sources "Third-party research" block, Step 0.7, Step 1 item 3, Step 10.1 / 10.5 self-audit / 10.6 log, and the `references/report_structure.md` Data Used manifest gains an "Institute research (local `db/zsxq.db`)" category. No machine-enforced prerequisite added — the zsxq scripts are invoked directly, read-only.)

Earlier 2026-06-09 (round 16 — **`theme-research` now mines the local zsxq library (`db/zsxq.db`) as a first-class source at create AND refresh — not only via `zsxq-ideas` theme-build.** Added a *Local zsxq report library* subsection to Data sources giving the three mechanical read-only steps (`zsxq-analyze/find_pdf.py` per-alias targeted surfacing + `zsxq-recommend/list_recent.py` recency window → `zsxq-ideas/evidence_bundle.py` manifest → `zsxq-analyze/ocr_pdf.py` + `extract_pdf.py` original-text read), the zsxq citation convention (`p.N` in the **link text** — the viewer does not auto-scroll on the `#page=N` fragment — + verbatim original-language quote, every number string-matched to its source; route is `/zsxq/pdf-viewer/`, the old `/zsxq-pdf/` form is a dead 404), and the explicit boundary vs `zsxq-ideas` (theme-build clusters the *whole feed* to seed several baskets; theme-research mines for *one named* theme). Wired into the workflow: **Step 1 (Create)** mines zsxq *before* web search; **Step 2 (Refresh)** re-mines since `Last refreshed` for new PT/rating calls + conviction re-ranks + TAM revisions; **Step 4b (Expansion)** gets the concrete strict-keyword command. New **`## Prerequisites`** block declares `[[zsxq-recommend]]` / `[[zsxq-analyze]]` / `[[zsxq-ideas]]` as the read-only data layer (no machine-enforced deps). Guardrail added: DB is read-only; the 翻译精华 `summary` is the triage layer (broker + rating + PT + thesis at a glance) but the load-bearing numbers (TAM anchor, PT derivations) come from the extracted original text, string-matched. Deliberately aligned with `company-research`'s round-17 zsxq integration — same `find_pdf.py` per-alias lookup, same `/zsxq/pdf-viewer/` route, same `p.N`-in-link-text rule. Frontmatter description + Data Used manifest updated.)

Earlier 2026-06-08 (round 15 — **NEW skill `market-status`.** A second macro / regime read, paired with [[market-complacency]] but anchored on the Goldman Sachs Weekly Kickstart's "Evaluating exuberance" framework (2026-06-05 issue). Scores the nine GS Exhibit-3 indicators across four categories (share-price action: Momentum factor 3M, S&P 500 52-week breadth; trading activity: GS Speculative Trading Indicator, CBOE Put/Call, median S&P 500 short interest; investor sentiment: Yale Stock Market Confidence Indices, AAII bull-bear; corporate sentiment: US IPO count, net US equity issuance) plus the broader Kickstart dashboard pages (GS US Equity Sentiment Indicator, GS US FCI proxy via NFCI, FedWatch 2026 YE, GDP nowcast, VIX / VIX3M, sector + factor returns, top-10 movers, top-30 concentration, CAPE / trailing PE, EPS revision breadth, 10Y / 2Y / TIPS yields). Each indicator gets a percentile rank vs ~30y history; composite 0–100 maps to a 5-tier verdict (**Frothy / Stretched / Elevated / Neutral / Subdued**). Hard data fetched via yfinance + FRED + multpl (MTUM/SPY momentum proxy, ARKK/IPO/BUZZ speculative-trade proxy, VIX, NFCI, Shiller CAPE, sector ETFs, factor ETFs, top-30 SPX movers). WebSearch fallback queue for the seven indicators with no clean public series (52-week breadth, GS Speculative Trading absolute level, equity put/call, median short interest, Yale Confidence, AAII weekly, IPO count, SIFMA net issuance) — script emits `_websearch_queue.json` which the agent resolves via WebSearch, then `merge_websearch.py` patches the summary JSON. Report saved to `reports/market-status/market_status_<YYYY-MM-DD>.md` (English only) with a GS-Kickstart-style verdict box, raw-HTML Figure 1 calibration table (Dot-Com / 2021 / Current columns colour-coded ms-red / ms-amber / ms-green), Share-prices / Trading-activity / Investor-sentiment / Corporate-sentiment narrative blocks, Macro & cross-asset panel, and a "What does today look like?" historical-anchor closer. First run (2026-06-07) prints: composite 59 (Elevated) / median 88 (Frothy) — Goldman's "above historical averages but below 2000/2021 peaks" call, with the unusual cross-section that positioning indicators (P/C, short interest, AAII) are *contra* — bottom three deciles. Distinct from [[market-complacency]]: complacency is credit-led "is risk under-priced"; market-status is bull-market-exuberance "is this dot-com again". Read-only against `db/indicators.db`; new caches in `oneoff/market_status_<DATE>_*.{csv,json}`; charts in `reports/charts/market_status_<DATE>_*.png`.)

Earlier 2026-06-07 (round 14 — **`catalyst-calendar` "Broker-style methodology reference" expanded with 5 more patterns** absorbed from a deeper sweep of US-equity weeklies in the zsxq library (Citi Bear Market Checklist, Barclays "Pricing a Capex Supercycle", Bernstein AWS Web Metrics, BofA Flow Show, DB Beige Book LLM scoring, DB ORCL F4Q preview). Additions: **§F.2 Citi BMC** — 18-indicator calibrated bear-market checklist with thresholds vs 2000 / 2007 peaks (currently US 11.5, post-2008 high; rule of thumb: ≥10 exuberance accumulating, ≥12 dip-buying stops working, ≥14 pre-top); paired with Goldman's qualitative 4-dim (§F.1). **§I Valuation-metric regime switch** — when S&P 500 capex/CFO > 40% AND capex YoY > 25%, switch from PE to EV/EBITDA / EV/EBIT (Barclays finding: rank correlation of EV/EBIT decile to forward 3y stock returns is much higher than PE during supercycles); historical episodes named (1990s internet, 2000s housing/energy, 2018+ cloud, 2024–26 AI). **§J Alt-data leading indicators** — 11-row table mapping alt-data series (AWS web traffic, OpenTable, card-spend, Placer.ai geo-data, Sensor Tower, box-office, EIA crude, TSMC monthly, Cass trucking, Smith Travel RevPAR, MBA mortgage apps) to the segment / single-name they forecast, with lead time and source; 3-cause-of-weakness framework (demand shift / supply constraint / pricing or cost change) for interpreting deviations. **§K Threshold-based historical rules library** — 12 "Bloomberg terminal tag" rules (CPI > 4% YoY → SPX −4%/3m −7%/6m; NFP > 125k 3M MA → 30y UST tests 5%; HY OAS < 300bp + VIX > 20 → equity vol mean-reverts in 5/6 cases; etc.) for flagging setups without fresh historical analysis. **§L LLM-as-tool scoring of qualitative Fed publications** — Beige Book / FOMC minutes / Powell pressers scored on calibrated 0–10 dimensions (inflation tone / labor tone / growth tone / recession concern / financial-conditions tone) Δ vs prior reading; explicit note that Claude-in-conversation does this scoring (not an LLM API call) per project rule. **§M Earnings-preview methodology beyond consensus** — DB's ORCL F4Q preview template: consensus + sub-segment split + capacity / delivery proxies (e.g. "400 MW compute delivered last Q") + contract-structure changes (renewal 2-4x, framework + min-commit clauses) + delivery-timing trends (60-90d → 90-120d = capacity-constrained, bullish) + multi-platform partnerships + read-across; populate ≥4 signal types per name. Mode B Step-2 gathers these explicitly.)

Earlier 2026-06-07 (round 13 — **`catalyst-calendar` gains a "Broker-style methodology reference" section.** Patterns absorbed from the Goldman / Nomura / Morgan Stanley / Citi week-ahead notes the user reads in the zsxq library — sell-side weeklies are vastly more granular than a calendar of dates with consensus numbers. New section covers: (A) sub-component watch lists for CPI / PCE / PPI / NFP / U-Mich / ISM / retail sales / GDP — every print must forecast 3+ sub-pieces mapped to causal mechanisms (tariff pass-through, energy fade, services stickiness, wage transmission), not labelled "hot/cold"; (B) Fed speaker hawk / dove map with stance + voting status + expected emphasis per speaker; (C) cross-asset confirmation framework — equity vol vs MOVE vs HY OAS vs DXY vs T5YIFR pattern tables for "equity vol freaked alone vs confirmed risk-off" reads; (D) curve trades + central-bank meeting probabilities — spot-level forecasts ALWAYS layered with curve structures (2/5 steepener, 5/30 flattener) and per-jurisdiction meeting probabilities (ECB Jul 25bp likely, BoJ Jun 95% priced); (E) "forward data" calendar with directional bias — every entry has a mechanism, not just a date; (F) Goldman's 4-dim "exuberance check" (price/breadth, speculation, sentiment, financing) + 3 historical bull-termination triggers (fundamentals weakening, IPO supply, Fed tightening) for late-cycle regime reads; (G) tariff / regulatory schedule tracker — Section 301 expiries, FDA AdComm, EU Phase II deadlines as discrete dated events; (H) fund flows + earnings revisions trackers as cross-cycle context. Mode A "Fed speakers" gains hawk/dove tagging requirement. Mode B Step-2 "Gather catalysts" must pull sub-component lists + tariff schedule. Mode B Step-4 weekly preview template restructured: per-release entry now has sub-component forecast + cross-asset confirmation + curve-trade-or-meeting-probability; "Forward data" + "Fund flows + earnings revisions" become explicit sections.)

Earlier 2026-06-07 (round 12 — **`catalyst-calendar` gains an "Event positioning lens — option / VIX / spread context" section.** Bull/bear thresholds on a macro print or earnings release are only half the picture; the other half is *what's already priced* into the option market. The new section codifies a 4-layer framework — what's priced (FedWatch / 2y yield), implied move (SPX 1DTE straddle / single-name straddle), directional bias (SKEW / put-call), cross-check (HY OAS / MOVE / 5y5y breakeven) — plus a per-indicator playbook (CPI / PPI / jobless claims / U-Mich / FOMC / NFP / PCE / ISM each with its primary tool and "too high" tell), an earnings implied-move framework (3-yr historical-realized vs implied → long-gamma-cheap or rich), a concrete "is the price too high?" decision rule, and a data-sources tie-in to `db/indicators.db` (which already tracks `vix` / `vvix` / `vix_slope` / `hy_oas` / `ig_oas` / `tnx` / `dxy` — needs `^MOVE` / `^SKEW` / VIX1D / 5y5y / 2y added separately). Modes A (day-of) and B (week-ahead) reference the section directly: Mode A's "What to cover" gains a tight "Market-pricing context" bullet; Mode B's Step-2 gathers positioning data per H-impact event; Mode B's Step-4 "Positioning implications" template now requires per-event breakdown (what's priced / implied move / vol-surface tell / cross-asset check / decision). Mode C (single-deal M&A) is exempt — spread math there is direct. Guardrails: never quote an implied move without an as-of timestamp; never fabricate FedWatch probabilities; never treat one tool's signal as the whole picture; flag cross-asset disagreement as a finding, not smooth it over.)

Earlier 2026-06-07 (round 11 — **Merged `morning-note` + `ma-event-tracker` into `catalyst-calendar`.** The three skills overlapped — all three answered some flavor of "what's coming up?" Now `catalyst-calendar` is a single skill with three modes dispatched off user phrasing: **(A) Day-of brief** — "what's big today/tomorrow", "morning note", "key events today" → 500–1,500 word opinionated brief covering macro (NFP / CPI / PCE / FOMC / ISM / jobless claims / retail sales), earnings pre/post, Fed speakers, M&A milestones, OPEX, index rebalances. In-chat by default, saved to `reports/morning/<YYYY-MM-DD>_<topic-slug>.md` only when the user asks. **(B) Week-ahead / horizon** — "catalyst calendar", "upcoming events", "what's coming this week" → multi-day table + weekly preview note at `reports/calendar/<week-of-YYYY-MM-DD>.md`. **(C) Single-deal M&A monitor** — "track the X-Y merger", "M&A status on <ticker>", "what's the spread on Z" → 3,000–6,000 word deep deal report at `reports/ma/<Target>_<Acquirer>_<YYYY-MM-DD>.md` (full ma-event-tracker workflow preserved: Step-0 target/acquirer direction confirmation, deal-snapshot YAML, spread math, milestone tracker, 5-category break-risk map, bear/base/bull probability range, Data Used manifest). Cross-cutting guardrails (citation standard, numerical accuracy, no destructive SQL, English/pinyin filename rule) shared across all modes. Cross-references in `regulatory-risk-monitor` updated from `[[ma-event-tracker]]` → `[[catalyst-calendar]]`. Old `.claude/skills/morning-note/` and `.claude/skills/ma-event-tracker/` directories removed.)

Earlier 2026-06-07 (round 10 — **NEW skill `market-complacency`.** A macro / regime read that scores how much risk the market is currently under-pricing across credit (HY / IG / CCC OAS), equity vol (VIX, VVIX, VIX term slope, SKEW), rate vol (MOVE), risk-premium compression (ERP, HYG/LQD), sentiment (AAII bull-bear, NAAIM exposure), and valuation (Shiller CAPE). Each indicator gets a 10y percentile rank with the direction flipped per indicator so that "lower percentile = more complacent" is consistent; the weighted composite (0–100) maps to a 5-tier verdict (**Stretched / Elevated / Neutral / Cautious / Panicked**). Report includes an indicator-by-indicator decile table, a cross-asset signature paragraph (which categories agree, which diverge), 6–10 charts, and a historical-precedents block showing SPY 6/12/24m forward returns from dates when the composite was within ±5 of today. Required indicators 1–10 must all fetch (HY/IG/CCC OAS, VIX, VVIX, VIX term slope, SKEW, MOVE, ERP, HYG/LQD); AAII/NAAIM/CAPE are optional with graceful degradation + composite re-normalization. Read-only against `db/indicators.db`; new indicators (CCC OAS, SKEW, MOVE, CAPE, AAII, NAAIM) cached as `oneoff/*.csv` rather than promoted to the live dashboard (separate engineering task). English-only output; saves to `reports/market-complacency/market_complacency_<YYYY-MM-DD>.md`.)

Earlier 2026-06-03 (round 9 — **`company-research` default flipped to Simplified Chinese only.** Previously the skill defaulted to producing both English and Chinese reports (two files per company). The new default is one Chinese file (`<Slug>_公司研究.md`); an English file is produced ONLY when the user explicitly opts in with `in English`, `English only`, `--lang en`, `--en-only`, `bilingual`, `also in English`, or similar. The Chinese report is now written with bilingual technical-term discipline (MANDATORY): financial metrics (`gross margin (毛利率)`, `EBITDA`, `ROIC`), industry terms (`semiconductor (半导体)`, `advanced packaging (先进封装)`, `GAA`), regulatory bodies (`MIIT (工信部)`, `CSRC (证监会)`), and named entities (`H200`, `Blackwell`, `S&P 500`, `Buy/Hold/Sell`) keep the English form alongside (or instead of) the Chinese gloss so citations remain cross-checkable against the underlying English sources. Existing English files from prior runs are NOT auto-refreshed unless the user explicitly opts in. SKILL.md description, "Report language" section, override table, Step 9, Step 10 verification, and the Update-in-place rule all updated; `references/investor_lenses.md` and `references/quality_checklist.md` updated to drop "both EN+ZH" assumption.)

Earlier 2026-06-02 (round 8 — **PT extraction lifted to a shared layer: `/zsxq-analyze` now also persists PTs, with `--replace` so the deep-PDF read overrides the summary-only row.** The two helpers (`persist_pts.py`, `missing_coverage.py`) moved out of the recommend-skill's private `scripts/` into the project-root `scripts/` so both skills invoke them by the same path. A new shared reference at [`reference/pt_extraction.md`](pt_extraction.md) owns the JSON record schema, the rating/currency vocabulary, and what to skip — both SKILL.mds link to it instead of duplicating the rules. The `/zsxq-recommend` path keeps default `INSERT OR IGNORE` semantics (first wins; the summary-only row is a best-effort placeholder), and `/zsxq-analyze` passes `--replace` so its higher-fidelity full-PDF + OCR + visual-page extraction overwrites that placeholder. `stock_price_target_db.upsert_target()` gained a `replace=False` kwarg; `scripts/persist_pts.py` gained a `--replace` flag; the JSON-summary output gained a `replaced` counter so users see overwrites vs no-ops at a glance.)

Earlier 2026-06-02 (round 7 — **`zsxq-recommend` gains Step 4 (Persist PT calls).** While the agent is already reading every recent summary to rank reads, it now also extracts sell-side price-target calls (broker × ticker × rating × PT × ccy + a one-line catalyst) and pipes them through `.claude/skills/zsxq-recommend/scripts/persist_pts.py` into `db/stock_price_target.db` — the same store the new `/pt` viewer surfaces. The script auto-fetches close + market cap on the report date via yfinance, computes upside%, and is idempotent on UNIQUE(ticker, broker, file_id) so re-running on the same window is a no-op. Choosing to merge rather than add a separate `zsxq-pt` skill saves the read-tokens cost of re-scanning the same summaries twice; if backfill across thousands of historical PDFs ever becomes a need, the offline path is a non-LLM regex script — not another skill.)

Earlier 2026-06-01 (round 6 — **`theme-research` gains Step 4b (Expansion refresh) + Citation audit + Retroactive baseline; `zsxq-ideas` gets strict-keyword refresh-mining + single-theme direct-edit guidance.** Captures the pattern that emerged when rebuilding ai-power (16→21 tickers, 19 reports) and memory-upcycle (14→16 tickers, 22 reports) from the OCR'd original PDFs: (1) **Step 4b Expansion refresh** — a refresh that widens sourcing + grows the basket; the only mode where Thesis gets rewritten during refresh, and only with user-confirmed adds. (2) **Citation audit** is now mandatory on every refresh — walk each prior `zsxq #<file_id>` citation against the actual PDF; this caught a real mis-attribution (`#812485545245152` was tagged "MS Hua Hong AAI" but is MS Tongrentang TCM). (3) **Retroactive snapshot baseline** — when an existing theme pre-dates the sidecar, write a reconstructed baseline + the current refresh in the same commit so the next refresh has a diff target. (4) **Chinese-companion-on-existing-EN** path — flip the EN file's `Languages tracked: en` → `en, zh` in the same commit. (5) **Strict-keyword refresh-mining** — for `zsxq-ideas` TB1, the loose keywords used for cluster-time over-match generic AI / data-center / 算力 reports; refresh-mining must use tracked-ticker names + theme-specific terms (HBM/NOR/SST/GLP-1) on a wider ~600-800-row window. (6) **Single-theme direct-edit** beats agent fan-out — the round-trip overhead and reconciliation cost outweigh the parallelism benefit when N=1. Reserve agents for genuine N-independent work.)

Earlier 2026-05-31 (round 5 — **`theme-research` gains a trackability layer.** Each theme stays ONE canonical `reports/themes/<slug>_theme.md` (no dated copies), now with a `## What's New` delta block pinned at the top (newest refresh first, older in a `<details>` archive) + a machine-readable `<slug>_theme.snapshots.jsonl` sidecar (one state-vector line per create/refresh/mutate). The next refresh set-diffs against the last snapshot line to auto-author What's New; git `show`/`log` gives full point-in-time recall. Answers "what did I know before, what's new?" without file sprawl. Refresh Step 4, Mutate Step 5, Verify Step 7, and the Output-Format block list all updated.)

Earlier 2026-05-31 (round 4 — **`zsxq-ideas` gains a third mode: theme-build.** Clusters the feed (reuse fishing F1–F3), then seeds/refreshes durable `theme-research` baskets at `reports/themes/<slug>_theme.md` from the *actual broker content* — not generic web knowledge. **Primary source = original PDF text** via `zsxq-analyze/extract_pdf.py` (OCR image-only bank PDFs first with `ocr_pdf.py`); the 翻译精华 summary column is a curated secondary source, fallback-only. New `scripts/evidence_bundle.py` builds the per-cluster extraction manifest (text-ready / OCR-cached / needs-OCR status + extract command per report). New **zsxq citation convention** (applies to all 3 modes): cite each broker number to its `file_id` AND page via `http://localhost:5001/zsxq-pdf/<file_id>#page=<N>` (`p.N` in the link text), quote the verbatim original-language source text alongside it, never the report title alone, and string-match every number against the extracted text — hard no-fabrication rule. Adds `[[theme-research]]` to the skill's Prerequisites. Triggers added: "build themes from zsxq", "turn my zsxq feed into baskets".)

Earlier 2026-05-31 (round 3.5): NEW skill **`zsxq-ideas`** — idea-generation funnel sourced from the zsxq report library. Two modes: themed (user names a theme → 8-12 parallel `/zsxq-analyze` fan-out → 5-10 ticker shortlist with idea-generation Step-4 presentation, saved to `reports/ideas/zsxq_<slug>_<date>.md`) and fishing (no theme → cluster the recent 200-row feed into 3-6 themes, then lite fan-out on top 2 themes for a triage shortlist). Promotes the zsxq area from "Pair" to "Trio" — `zsxq-recommend` + `zsxq-analyze` + `zsxq-ideas`. English filenames mandatory per the CLAUDE.md rule. Output is idea sourcing, not a buy rating — next step always points to `/company-research <ticker>`.

Earlier 2026-05-31: Language-default flip on the 4 monitoring/tracking skills — `etf-overlap`, `ma-event-tracker`, `theme-research`, `regulatory-risk-monitor` now default to **English only**; Chinese is opt-in via `also in Chinese` / `bilingual` / `--zh` / `用中文也输出一份`. The substantive research skills `compare-companies` / `earnings-analysis` / `sector-overview` keep their bilingual default. `company-research` was later flipped (see round 9 above) to **Chinese-only default**. Same English-only line applied to the earlier-added `take-profit-lab`. Recorded in [feedback_tracking_skills_english_default.md](../../.claude/projects/-Users-x-projects-financial-agent/memory/feedback_tracking_skills_english_default.md).

Earlier 2026-05-31 round 3 — investor-lens pack expansion: `references/investor_lenses.md` now covers **9 lenses** (4 core + 5 optional). Core (default): 10.1 Buffett, 10.2 Munger, 10.3 Damodaran, 10.4 Howard Marks cycle. New optional packs: **10.5 Lynch GARP** (mid-cap growers / ten-bagger discipline / Lynch six-categories), **10.6 Fisher scuttlebutt** (15-point qualitative growth + mandatory non-filing evidence note), **10.7 Burry forensic deep value** (hated-sector + downside-first; sum-to-12 scoring), **10.8 Druckenmiller liquidity-regime** (macro context + 3:1 R/R + same-day-exit trigger), **10.9 Cathie Wood Wright's Law** (cost-curve + 5yr TAM re-pricing + convergence). Routing rules in `investor_lenses.md` § "Implementation tips" tell the analyst which optional lenses fit which company type. Quality checklist + report_structure.md updated with per-lens required-block specs.

Earlier 2026-05-31 round 2: four NEW skills added — (B) **`etf-overlap`** — head-to-head ETF holdings overlap for 2–4 ETFs; SEC N-PORT primary, issuer-CSV daily fallback, yfinance last-resort. (C) **`ma-event-tracker`** — single-deal merger tracking with spread / milestones / break-risk / probability range. (D) **`theme-research`** — tracked thematic baskets backed by `reports/themes/<slug>/registry.yaml`. (E) **`regulatory-risk-monitor`** — single-case regulatory tracking (FDA / DOJ / FTC / EU / SAMR / CSRC / etc.). All four bilingual default.

Earlier 2026-05-31 round 1: (1) **`company-research`** gains an optional Section 10 — Investor-lens scorecards (Buffett / Munger / Damodaran / Howard Marks cycle) in `references/investor_lenses.md`, plumbed to `indicators.db`. (2) **NEW skill `take-profit-lab`** — exit-discipline backtest. (3) All four research skills gain `## Guardrails` + `## Data Used` manifests.

Earlier: 2026-05-31 (`compare-companies` N-way support 2-4 companies; prior-research existence check hardened)

---

## 1. Trading-analysis pipeline (the spine — explicit dependencies)

This is the only chain with **machine-enforced** upstream skills (declared via `[[wikilink]]` references in each SKILL.md's `## Prerequisites` block). Each downstream skill auto-cascades upstream if its prerequisite is missing.

```
                  ┌──────────────────┐
                  │ trading-analysis │  ← orchestrator (runs whole chain)
                  └────────┬─────────┘
                           │ fans out 3 analysts in parallel
        ┌──────────────────┼──────────────────────────────┐
        ▼                  ▼                              ▼
┌──────────────────┐ ┌─────────────┐         ┌────────────────────┐
│ sentiment-analyst│ │ news-analyst│         │  company-research  │
└────────┬─────────┘ └──────┬──────┘         └──────────┬─────────┘
         │                  │                           ▲
         │                  │            (US issuers,   │
         │                  │             Step 0.5)     │
         │                  │                ┌──────────┴─────────┐
         │                  │                │ sec-report-summary │
         │                  │                └────────────────────┘
         │                  │                           │
         └──────────────────┴───────────────────────────┘
                            ▼
                  ┌───────────────────┐
                  │ bull-bear-debate  │  needs all 3 analyst reports
                  └─────────┬─────────┘
                            ▼
                  ┌───────────────────┐
                  │ research-manager  │  consumes debate transcript
                  └─────────┬─────────┘
                            ▼
                  ┌───────────────────┐
                  │   trader-plan     │  Buy/Hold/Sell proposal
                  └─────────┬─────────┘
                            ▼
                  ┌───────────────────┐
                  │   risk-debate     │  3-way Aggressive/Conservative/Neutral
                  └─────────┬─────────┘
                            ▼
                  ┌───────────────────┐
                  │ portfolio-decision│  final 5-tier rating (terminal)
                  └───────────────────┘
```

| Skill | Prereqs | Output |
|---|---|---|
| [trading-analysis](../.claude/skills/trading-analysis/SKILL.md) | — (orchestrator) | Runs the whole pipeline end-to-end |
| [sentiment-analyst](../.claude/skills/sentiment-analyst/SKILL.md) | — | 7-day sentiment report (Yahoo / StockTwits / Reddit) |
| [news-analyst](../.claude/skills/news-analyst/SKILL.md) | — | Macro + ticker news, past 30 days |
| [sec-report-summary](../.claude/skills/sec-report-summary/SKILL.md) | — (US-only sub-skill) | Multi-year SEC filing narrative |
| [company-research](../.claude/skills/company-research/SKILL.md) | `sec-report-summary` (US issuers only, Step 0.5) | 6–10k word deep dive — **Simplified Chinese by default** (technical terms keep English alongside Chinese gloss); English produced only when the user explicitly opts in (`in English`, `bilingual`, `also in English`) |
| [bull-bear-debate](../.claude/skills/bull-bear-debate/SKILL.md) | 3 analyst reports | Multi-round debate transcript |
| [research-manager](../.claude/skills/research-manager/SKILL.md) | `bull-bear-debate` | ResearchPlan + 5-tier rating |
| [trader-plan](../.claude/skills/trader-plan/SKILL.md) | `research-manager` | Buy/Hold/Sell proposal |
| [risk-debate](../.claude/skills/risk-debate/SKILL.md) | `trader-plan` + 3 analyst reports | 3-way risk transcript |
| [portfolio-decision](../.claude/skills/portfolio-decision/SKILL.md) | `risk-debate`, `trader-plan`, `research-manager` | Final rating (terminal) |

## 2. Trio: zsxq report library

```
┌──────────────────┐     file_id     ┌──────────────────┐
│ zsxq-recommend   │ ─────────────▶  │   zsxq-analyze   │
│ (find PDFs)      │                 │ (deep-read 1 PDF)│
└────────┬─────────┘                 └─────────┬────────┘
         │           orchestrates both         │
         │     ┌───────────────────────────────┘
         ▼     ▼
    ┌──────────────────┐
    │   zsxq-ideas     │  ← idea-generation funnel from zsxq feed
    │ (themed/fishing) │     (fans out N parallel /zsxq-analyze)
    └──────────────────┘
```

All three read `db/zsxq.db`. `zsxq-recommend` reads metadata only; `zsxq-analyze` extracts the full PDF text (via `ocrmac` if needed); `zsxq-ideas` orchestrates both into either a 5-10 ticker shortlist with `idea-generation`'s Step-4 presentation (saved to `reports/ideas/zsxq_<slug>_<date>.md`), or — in **theme-build mode** — durable `theme-research` baskets seeded from the cluster's actual broker content (`reports/themes/<slug>_theme.md`). As of round 16, **`theme-research` also reads `db/zsxq.db` directly** via the same read-only scripts (`list_recent.py` / `evidence_bundle.py` / `extract_pdf.py` / `ocr_pdf.py`): `zsxq-ideas` theme-build clusters the *whole feed* to seed several baskets at once, while `theme-research` mines the library for *one named* theme at create/refresh. So there are now two zsxq paths into a basket — feed-clustering (zsxq-ideas) and single-theme mining (theme-research) — both writing the same file format with the same page-anchored citation discipline.

| Skill | Purpose |
|---|---|
| [zsxq-recommend](../.claude/skills/zsxq-recommend/SKILL.md) | Surface candidate `file_id`s from the recent feed |
| [zsxq-analyze](../.claude/skills/zsxq-analyze/SKILL.md) | Deep-read one PDF and answer a question about it |
| [zsxq-ideas](../.claude/skills/zsxq-ideas/SKILL.md) | Generate ideas / baskets from the zsxq feed — **themed** ("AI infra ideas from zsxq"), **fishing** ("what should I buy / pitch me something"), or **theme-build** ("build themes from zsxq" → seeds/refreshes `theme-research` baskets from real broker content via `evidence_bundle.py` + the zsxq citation convention). Themed/fishing save to `reports/ideas/`; theme-build hands off to `theme-research` at `reports/themes/` |

## 3. Coverage-lifecycle workflows (no hard deps — logical order)

```
idea-generation → initiating-coverage → earnings-preview → earnings-analysis → model-update
                       │                                                            │
                       └─────── builds on ───┐                                       │
                                             ▼                                       ▼
                                     company-research                         thesis-tracker
                                             │
                                             └── consumed by ──▶ compare-companies (head-to-head)
```

- [initiating-coverage](../.claude/skills/initiating-coverage/SKILL.md) is a 5-task workflow whose Task-1 is essentially `company-research`.
- [earnings-preview](../.claude/skills/earnings-preview/SKILL.md) → [earnings-analysis](../.claude/skills/earnings-analysis/SKILL.md) → [model-update](../.claude/skills/model-update/SKILL.md) is the quarterly cycle for a name already under coverage.
- [thesis-tracker](../.claude/skills/thesis-tracker/SKILL.md) is the long-running journal that consumes results from the others.
- [idea-generation](../.claude/skills/idea-generation/SKILL.md) seeds the funnel via generic quantitative / thematic / special-situation screens. [zsxq-ideas](../.claude/skills/zsxq-ideas/SKILL.md) seeds the same funnel from the zsxq library instead (themed or fishing). Both feed downstream `company-research` → `initiating-coverage` / `trading-analysis`.
- [compare-companies](../.claude/skills/compare-companies/SKILL.md) takes **2 to 4** `company-research` outputs and produces an N-way head-to-head comparison focused on the delta — product overlap matrix (N+1 columns), moat anatomy (N-column tables), customer overlap, dimension-by-dimension scorecard (rank-based for N≥3). **Default behavior: two files per tuple — English at `reports/compare/<A>_vs_<B>[_vs_<C>...].md` and Simplified Chinese at `reports/compare/<...>_zh.md`** (5–9k words for N=2; 7–12k for N=3; 10–15k for N=4; natively authored in each language). Users can override to single-language with `--en-only` / `--zh-only`. If a research doc is missing or stale, it invokes `company-research` on the missing side first. N≥5 not supported — split into multiple pairwise reports or use `sector-overview`.

## 4. Standalone, no dependencies

| Skill | Purpose |
|---|---|
| [sector-overview](../.claude/skills/sector-overview/SKILL.md) | Industry-level landscape report |
| [take-profit-lab](../.claude/skills/take-profit-lab/SKILL.md) | Exit-discipline backtest on a single ticker (hold / tier / trailing stop / vol-aware); complements `trader-plan` (entry) and `portfolio-decision` (final rating) — owns the *exit* question |
| [market-complacency](../.claude/skills/market-complacency/SKILL.md) | Macro / regime read — composite "complacency dashboard" of credit spreads (HY / IG / CCC OAS), equity vol (VIX, VVIX, term slope, SKEW), rate vol (MOVE), risk-premium compression (ERP, HYG/LQD), sentiment (AAII / NAAIM), valuation (Shiller CAPE). 10y percentile-rank per indicator, weighted composite 0–100, 5-tier verdict (**Stretched / Elevated / Neutral / Cautious / Panicked**), historical-precedents block with SPY forward returns. Read-only against `db/indicators.db`. **English-only** |
| [market-status](../.claude/skills/market-status/SKILL.md) | Macro / regime read — Goldman Sachs Weekly Kickstart-style exuberance dashboard. Nine GS Exhibit-3 indicators (Momentum factor / S&P 500 52-week breadth / GS Speculative Trading / CBOE Put-Call / median short interest / Yale Confidence / AAII / IPO count / net equity issuance) + broader Kickstart panel (GS Sentiment Indicator / NFCI proxy for US FCI / FedWatch / GDP nowcast / VIX-VIX3M / sector + factor returns / top movers / CAPE / EPS revision breadth). Hard-data via yfinance + FRED + multpl; WebSearch fallback queue + `merge_websearch.py` patcher for indicators without clean public series. Composite 0–100, 5-tier verdict (**Frothy / Stretched / Elevated / Neutral / Subdued**), raw-HTML Figure 1 calibration table (Dot-Com / 2021 / Current), historical-anchor closer. Complements `market-complacency` — same axis, different lens (price-led exuberance vs credit-led complacency). **English-only** |
| [etf-overlap](../.claude/skills/etf-overlap/SKILL.md) | Head-to-head holdings overlap for 2–4 ETFs — duplicative / complementary / orthogonal verdict, sector skew, top common positions; SEC N-PORT primary, issuer CSV daily, yfinance fallback. **English default; Chinese opt-in** |
| [theme-research](../.claude/skills/theme-research/SKILL.md) | Tracked thematic baskets — one canonical `reports/themes/<slug>_theme.md` (Tracked tickers table + thesis + performance + drift signals) + a `## What's New` delta block + a `<slug>_theme.snapshots.jsonl` machine-snapshot sidecar for "what did I know before, what's new?" trackability (no dated copies); create / refresh / mutate / list modes; drift detection on theme-membership stability. **English default; Chinese opt-in** |
| [regulatory-risk-monitor](../.claude/skills/regulatory-risk-monitor/SKILL.md) | Single-case regulatory tracking on one ticker — FDA AdComm / DOJ-FTC antitrust / EU DG-COMP / SAMR / CSRC / EPA / FCC; evidence timeline + exposure map + scenario triggers. **English default; Chinese opt-in** |
| [canslim-screener](/Users/x/.claude/skills/canslim-screener/SKILL.md) | William O'Neil CANSLIM screen (lives in `~/.claude`, not project) |
| [catalyst-calendar](../.claude/skills/catalyst-calendar/SKILL.md) | One unified catalyst & event lens — three modes dispatched off user phrasing. **(A) Day-of brief** ("what's big today/tomorrow", "morning note") → opinionated 500–1,500 word brief on macro releases + earnings + Fed speakers + M&A milestones + OPEX; in-chat default. **(B) Week-ahead horizon** ("catalyst calendar", "upcoming events this week") → multi-day calendar table + weekly preview at `reports/calendar/`. **(C) Single-deal M&A monitor** ("track the X-Y merger", "spread on Z deal") → 3,000–6,000 word deep report at `reports/ma/<Target>_<Acquirer>_<YYYY-MM-DD>.md` with target/acquirer direction confirmation, deal-snapshot YAML, milestone tracker, 5-category break-risk map, scenario probability range. Absorbs the former `morning-note` (mode A) and `ma-event-tracker` (mode C). **English default; Chinese opt-in (mode C only)** |
| [earnings-upload-to-db](../.claude/skills/earnings-upload-to-db/SKILL.md) | PDF ingest into `db/notes.db` (data-plumbing, not analysis) |
| [build-knowledge-graph](../.claude/skills/build-knowledge-graph/SKILL.md) | Mine entities + `COMPETES_WITH` / `SUPPLIES` edges from `reports/**/*.md` into `db/graph_mirror.db` via `manual_graph.py`. Agent (Claude in conversation) does the reading; **no LLM API**. Cap ≤10 active edges per entity. |

---

## Key observations

- **Only the trading-analysis chain has machine-enforced deps** (the `## Prerequisites` blocks with `[[…]]` wikilinks). Everything else is independent — run them in any order.
- **`company-research` is the most reused artifact** — the trading pipeline, `initiating-coverage` (Task 1), and `compare-companies` (N per N-way comparison, where N ∈ {2, 3, 4}) all build on it.
- **`sec-report-summary` is conditional**: it's a Step-0.5 sub-skill of `company-research` for US issuers only; for non-US issuers (China A-share / HK / Taiwan / Japan / Korea), `company-research` skips it and builds the historical-evolution threads directly from domicile-portal filings. It can also be invoked standalone if the user just wants the SEC narrative without the full deep dive.
- **`trading-analysis` is the only true orchestrator**; `initiating-coverage` is 5 sequential tasks the user runs explicitly, not a one-shot pipeline.
- **Two data-source skills feed everything else indirectly**: `earnings-upload-to-db` → `db/notes.db`, and `zsxq-recommend` / `zsxq-analyze` → `db/zsxq.db`. They're not called by other skills, but the artifacts they produce are read by analysts.

---

## Maintenance

Update this file when any of the following change:

1. A skill is **added** under `.claude/skills/` — add a row to the relevant section.
2. A skill is **removed** — delete its row and any references.
3. A skill's **`## Prerequisites`** block changes (new `[[wikilink]]` upstream skill, or one removed) — update the arrow in section 1.
4. The orchestrator behavior of [trading-analysis](../.claude/skills/trading-analysis/SKILL.md) changes (e.g. a new analyst joins the parallel fan-out).

Also bump the "Last updated" date at the top.
